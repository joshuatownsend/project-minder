# TODO

## Performance

- [ ] **Performance P3 — RSC + TanStack Query + SSE** — The final performance phase; only viable now that P2 (SQLite, default-on) is shipped. (1) **Convert read-heavy pages to async RSC**: `src/app/{sessions,usage,stats,agents,skills,commands,insights,manual-steps,templates,config}/page.tsx` drop `"use client"`, become async RSCs that call `getDb()` directly with no fetch hop; small client islands handle filters/interactions. `src/app/sessions/[sessionId]/page.tsx` RSC fetches turn metadata; full text streamed via `<Suspense>`. Add `loading.tsx` per route for skeleton streaming. (2) **`@tanstack/react-query` v5 + `HydrationBoundary`**: wrap `layout.tsx` in `QueryProvider`; RSC dehydrates, client rehydrates. `staleTime: 30_000`, `gcTime: 5 * 60_000`, `refetchOnWindowFocus: false`. The 13 hooks in `src/hooks/` collapse into one `useQuery` per resource. (3) **Hover prefetch**: new `useHoverPrefetch(href, queryKey)` on `<Link>` calls `queryClient.prefetchQuery` on `onMouseEnter`; apply in `AppNav`, `ProjectCard`, `SessionsBrowser` rows. (4) **Server Actions for mutations**: replace POST routes for `toggleManualStep`, `setProjectStatus`, `pinProject`, `applyTemplate` with `'use server'` actions + `useMutation` optimistic updates. (5) **One SSE stream replaces all polling**: new `src/app/api/events/route.ts` emits `session.created/updated/removed`, `project.scanned`, `catalog.changed`, `config.changed`, `indexer.progress`; client handler calls `queryClient.invalidateQueries`; removes the `/api/pulse` poller. **New deps**: `@tanstack/react-query`, `@tanstack/react-query-devtools` (dev-only). **Risks**: RSC requires SQLite fallback path stays in façade; SSE on Windows behind some proxies needs flush headers (feature-detect + fall back to short poll); hydration timestamp mismatch (render ISO server-side, format with `Intl.RelativeTimeFormat` client-side). **Estimated impact**: first-load TTI on `/sessions`+`/usage` −50–70 %; cross-page nav feels instant; browser memory stable under Query GC. **Plan doc**: `C:\Users\joshu\.claude\plans\performance-of-project-minder-playful-river.md` §P3, lines 190–216.

## Housekeeping

- [ ] **Capture a real worktree-overlay shot as `worktrees.png`** — Sub-follow-up to the gh-pages publish work. The current capture-script run produces `todos-tab.png`, which honestly shows the TODOs tab. There's no live `worktrees.png` until we can capture a project that actually has an active Claude Code worktree overlay. Options: (a) extend `capture-screenshots.mjs` to scan `C:\dev\` for any project with a sibling `*--claude-worktrees-*` directory and shoot that detail page; (b) have the script temporarily `git worktree add` a demo worktree on Project Minder itself (e.g. `C:\dev\project-minder--claude-worktrees-demo`) before the capture and tear it down after. Option (a) is cleaner — it captures the feature as it appears in the wild. Once captured, restore the `worktrees.png` reference in `site/index.html` (currently shows `todos-tab.png`).
- [ ] **Bonus: PR check that warns when `site/screenshots/*.png` changes without `site/index.html`** — Sub-follow-up to the gh-pages publish work. A typical accident pattern: contributor regenerates screenshots but forgets to update the prose. Add a small workflow that runs on PRs touching `site/**` and fails (or warns via PR comment) if the diff contains `site/screenshots/*.png` changes but no `site/index.html` diff. Light-touch — a single grep over `gh pr diff --name-only`.
- [ ] **Prod-runtime `ERR_INVALID_ARG_TYPE: path must be string, received number (682512)`** — Found 2026-05-16 during the first end-to-end run of `npm run capture:docs:prod`. The orchestrator's build/start/poll/capture/teardown cycle works structurally (13/14 captures produced, taskkill cleanup succeeded), but most prod-mode shots render mostly-empty pages because compiled `.next/server/chunks/_09iqote._.js:1:31138` errors out on a `path` arg that's a number (`682512` repeats — looks like an inode-ish value). Stack trace points into Turbopack's bundled chunks (`eu` → `ey` → `eg` → `ek` → `eU` → `eD`). PNGs from the run were ~50–70% smaller than dev-mode equivalents (e.g. `memory.png` 144KB → 38KB), so the dashboard data is failing to load. Pure dev-mode captures still work fine. Likely culprits to investigate: any `path.join(scanResult.size, ...)` / `path.resolve(stat.ino, ...)` mistake (we pass file stats around in `src/lib/scanner/`); or a serialized RSC prop hitting a `fs.readFile(<number>)` somewhere. Repro: `npm run build && npm run start` then visit `/`. Until fixed, prefer dev captures (`npm run capture:docs:all`) and accept the residual skeleton flake on `/usage`.

### Simplification follow-ups

- [ ] **`runCatalogLint` re-walks project commands** — `src/lib/scanner/catalogLint.ts` calls `walkProjectCommands` for every project *again* after `scanProject` already called it per-project (inside `configLintPromise`). The catalog-lint pass needs project commands for cross-scope duplicate-slug detection, but currently does redundant fs-walks. Fix: thread the per-project `CommandEntry[]` arrays out of `scanProject` into `ScanResult`, then pass them into `runCatalogLint` so it can skip the re-walk. Symptom: pre-commit test-suite timeout bumps on machines with 50+ projects.

## Memory Observatory

*Inspired by [Nicolas Bustamante's "Agent Memory Engineering"](https://x.com/nicbstme/status/2050301124314563025), comparing how Claude Code, Codex CLI, and Hermes implement agent memory. Key findings: clever architectures (vector DBs, knowledge graphs, dedicated memory agents) all lost to **LLM + typed markdown + bash**; the interesting axis is the read/write **discipline** the agent follows, not the data structure; memory is a **hint surface, not authority** — every body read should be wrapped in an age-aware "verify before recommending" reminder. Phase 1 (Freshness + Telemetry + Budget + MEMORY.md index awareness on `/memory`) is in active planning per the approved plan at `~/.claude/plans/i-recently-read-this-temporal-crane.md`; the entries below are the deferred follow-on phases, in priority order.*

- [ ] **Wave M.2 — Cross-harness memory bridge (Codex + Gemini)** — **Status (2026-05-12):** deferred after Phase 1 + M.1 + M.3 + M.4 shipped (PRs #118 / #119 / #121). M.2 is the only remaining Memory Observatory wave. **Plan doc**: see `C:\Users\joshu\.claude\plans\i-recently-read-this-temporal-crane.md` for the upstream framing (Nicolas Bustamante's "Agent Memory Engineering" article comparing Claude Code / Codex CLI / Hermes memory layers), the article principles → Project Minder opportunities table, and the Phase 1 → M.1 → M.4 → M.2 dependency rationale. The plan doc and this TODO entry are intentionally redundant on the M.2 spec itself; the plan doc adds the *why* (article framing, cross-wave dependency story) that's not duplicated here. Addresses the article's central frustration: memory rules taught to Claude Code don't transfer to Codex / Gemini despite sharing the same filesystem, because each model is post-trained on its harness's specific memory layer (Claude Code's typed taxonomy + always-loaded MEMORY.md index + age-aware `<system-reminder>` framing on every body read vs Codex's offline extract+consolidate pipeline + lazy `memory_summary.md` load + on-demand handbook reads). Extend the existing adapter registry from session-read to full memory IO, then ship a diff/sync UI so canonical rules can be authored once and pushed across stores. **Upstream research needed before PLAN**: (a) **Codex memory layout** — per the article, Codex's memory folder has its own `.git/`, a `memory_summary.md` (5K-token cap, injected per turn), a full `MEMORY.md` handbook, and a `skills/` tree; discover exact path under `$CODEX_HOME/`; document the `MemoriesUsageKind` enum values (`MemoryMd`, `MemorySummary`, `RawMemories`, `RolloutSummaries`, `Skills`) so the bridge can expose per-kind usage. (b) **Gemini memory layout** — less documented; audit `~/.gemini/` for any memory equivalent on Windows; may need read-only first if Gemini has no equivalent yet. (c) **Taxonomy mapping** — Claude's `feedback_*` / `project_*` / `reference_*` / `user_*` may not align 1:1 with Codex's structure; define a canonical internal type set and a per-adapter encoder/decoder. **Interface extension**: extend `SessionAdapter` in `src/lib/adapters/types.ts` with `discoverMemory(opts?: { projectPath?: string }): Promise<MemoryEntry[]>`, `writeMemory(entry: MemoryEntry): Promise<MemoryWriteResult>`, and a `capabilities: { memoryRead: boolean; memoryWrite: boolean }` flag so the UI can grey-out write actions on adapters that don't support them. **UI**: new page `/memory/bridge` with a three-column layout (Claude | Codex | Gemini), each row showing the entry's presence across stores. Filter "Only show divergent" (exists in one store but not others, or bodies differ). Per-row actions: `Push to Codex`, `Push to Gemini`, `Pull from Codex`, etc., each using the target adapter's `writeMemory()` with format translation. Conflict UI: side-by-side diff with manual resolution before write. **Critical files**: `src/lib/adapters/types.ts` (extend interface; define adapter-agnostic `MemoryEntry` shape), `src/lib/adapters/claude.ts` (implement `discoverMemory()` / `writeMemory()` as thin wrappers over `src/lib/scanner/memoryWriter.ts`), `src/lib/adapters/codex.ts` (NEW work based on upstream research), `src/lib/adapters/gemini.ts` (NEW work or mark `memoryWrite: false` if upstream lacks it), `src/lib/memory/bridgeDiff.ts` (NEW — cross-adapter join + diff), `src/app/memory/bridge/page.tsx` (NEW), `src/app/api/memory/bridge/route.ts` (NEW — `GET` returns diff, `POST` performs a push), `src/components/MemoryBridge.tsx` (NEW — three-column UI). **Open questions to resolve in DISCUSS**: split into read-only first (M.2a) and write-mode second (M.2b)? how to handle harness-specific body syntax (e.g., Claude's `<system-reminder>` framing) when pushing to Codex — strip, transform, or warn? per-store last-modified for "newer side wins" auto-resolution, or always require manual conflict? per-project bridge or user-scope only initially? **Dependencies**: Phase 1 (read telemetry + index parsing) and ideally M.1 (seed generator) ship first — the bridge needs a canonical view of "what should be in every store." **Roadmap order**: ships after M.1. Ref: article §"Why memory does not transfer cleanly between agents" + §"Codex: only the index, full handbook on demand" + §"Claude Code: full index always loaded, bodies on demand."
