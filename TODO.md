# TODO

## Claude Code schema alignment (v2.1.202 → 2.1.222)

*Full spec + status table: `docs/superpowers/plans/2026-08-04-claude-code-schema-alignment.md`. Driver: ~20 upstream releases widened the transcript schema, and Minder decodes none of it. Measured on 2026-08-04 across the 30 most-recent `~/.claude/projects/*.jsonl` files — `effort` on 6979 assistant turns, `attributionMcpServer`/`attributionMcpTool` on 1782, `attributionSkill` on 920, plus three **new entry types** Minder's `entry.type` switches drop on the floor: `pr-link` (788), `ai-title` (1367), `permission-mode` (1358). All retroactive across existing history. Key design call: the new `attribution*` fields are **causal cost attribution** on assistant turns, semantically distinct from the existing inferred `tool_uses.skill_name`/`mcp_server` call counts — they go on `turns`, both stay, and that split is what fixes the MCP over-attribution bug Anthropic fixed upstream in 2.1.222.*

- [x] **B1 — Pricing correctness.** Shipped 2026-08-04 on `b1-pricing-fallback-refresh`. Grew beyond the planned fallback-table refresh, with approval: probing for a fast-mode marker surfaced that **95.5% of cache-creation tokens are 1-hour-TTL writes billed at 2×**, while Minder priced every cache write at the 1.25× 5-minute rate — a **$4,139 (14.9%) understatement** across the full corpus, affecting every session rather than only the offline path. Also corrected: modern Opus billed at the legacy $15/$75 via substring nesting (`claude-opus-4-8` → `claude-opus-4`), 200k reported as the context window for every 1M model, `applyPricingOverlay` silently dropping the >200k tiered rates, and three drifted hand-rolled copies of the cost formula. `DERIVED_VERSION` deliberately **not** bumped — stored `cost_usd` stays stale until A1's single re-parse.
  - [ ] **Follow-up — make pricing *consume* `usage.speed`.** Half done in A1: `speed` is now carried on `UsageTurn` by both readers and the ingest path, so it rides the shared re-parse and no second one is needed. What remains is pricing actually using it — `getModelPricing`/`applyPricing` still bill a fast-mode turn at standard rates, and fast mode on Opus 5 / 4.8 is $10/$50 per Mtok versus $5/$25, i.e. double. Needs a fast-rate column per model in `ModelPricing`, which is pricing-data work rather than decode work. **Currently unmeasurable locally:** across 1,200 transcripts / 10,742 assistant turns, `speed` was `standard` (10,288) or `null` (454) and **never `fast`** — so there is no local fixture to validate against and no present-day cost impact to correct. Worth building before it silently matters, not urgent.
- [x] **A1 — Transcript decode + migration 20 + `DERIVED_VERSION` 12→13.** Shipped 2026-08-05 on `a1-transcript-decode`. Both readers plus the DB write/read paths, 9 new tests. Probing 1,200 real transcripts first corrected three things the plan had wrong: there are **nine** unhandled entry types (not three); `sessionKind`/`entrypoint` ride **`attachment`** entries rather than assistant turns; and `ai-title`/`permission-mode` entries carry **no timestamp**, so the decode had to sit above the readers' `!entry.timestamp` guard or every one would be dropped silently. `effort` is on 10,288 of 10,742 assistant turns.
  - [ ] **Remaining — run the re-index on the live DB.** The *schema* half is verified on real data: migration 20 applied to a byte-copy of the live 1.19 GB `index.db` in **332 ms**, `quick_check: ok`, idempotent on re-run. What is left is the `DERIVED_VERSION` 13 re-parse, covering **4,998 sessions / 163,685 turns** — all stale at v13. Not run in the A1 session because a dev server was live on port 4100 holding the DB, so it meant restarting a running server. `tests/sqlSchemaSnapshot.test.ts` fails locally until the real DB migrates (it skips on CI, where there is no live DB) — environmental, not a code defect. A2–A6 all ride this single re-parse.
- [ ] **A2 — Effort analytics.** Cost and one-shot rate by reasoning effort. The question worth answering: does `high` buy a better outcome or just a larger bill? Explicit **Unknown** bucket for pre-2.1.212 turns — never folded into `medium`.
- [ ] **A3 — `sessionKind` segmentation.** `interactive` / `attached` / `unattended` (observed `"bg"`). Minder currently pools supervised sessions with unattended agent runs, which skews every average it reports.
- [ ] **A4 — Authoritative skill + MCP cost attribution.** Explicit fields for spend, existing inference for call counts. PR must report how much per-server attribution actually moved — if ~0, say so rather than claim a fix.
- [ ] **A5 — Authoritative PR linkage.** Prefer `type:"pr-link"` entries over `prExtractor.ts`'s regex scrape; same `session_prs` table, same consumers, only the source changes. Picks up 2.1.222's fixes for PRs created after branch push and via the REST API — cases the regex structurally cannot see. Narrows (does not close) the dangling `prSessionLinks` gap named in the Cloud session ingest entry below.
- [ ] **A6 — Hook performance + permission/denial analytics.** `hookInfos` carries `{command, durationMs}` — real hook latency (a 1450 ms `codegraph sync` on every prompt is exactly the self-inflicted latency this dashboard exists to expose). Plus `toolDenialKind`, the `permission-mode` timeline, and `promptSource`. Optional extra: prefer the CLI's own `aiTitle` over Minder's generated titles and skip the generation cost entirely.
- [ ] **C1 — `.claude/workflows/` catalog.** New `walkWorkflows.ts` + `/workflows` route + `workflowCatalog` flag. Statically parse the `export const meta` literal — never execute the script. Namespace as `ClaudeWorkflowEntry`; `scanner/cicd.ts` already owns "workflows" for GitHub Actions.
- [ ] **C2 — Runaway-delegation guardrail badges.** 2.1.212/217 added hard caps (200 spawns/session, 200 web searches, 20 concurrent, depth 3, `--max-budget-usd`). New failure mode: a session can be silently truncated by a cap rather than finishing. Minder already parses subagent trees; badge sessions near a cap and expose it to `notificationRules`.
- [ ] **C3 — OTEL `tool_source` / `message.uuid` correlation.** `tool_source` gives tool provenance without name inference; `message.uuid` is the missing join key between OTEL rows and transcript turns. Needs its own migration (21).
- [ ] **C4 — Hook + skill frontmatter parity.** Add the `DirectoryAdded` hook event. **Live bug:** `parseFrontmatter.ts` doesn't accept `yes`/`no`/`on`/`off` as booleans (2.1.218), so a skill using `on` parses wrong today. Also parse `context: fork`, `background`, `disable-model-invocation`, and verify `walkPlugins.ts` handles a plugin-root `SKILL.md` (`skills: "."`).

## Project Groups — one project, many locations

*Full spec + status table: `docs/superpowers/plans/2026-07-20-project-groups-multi-location.md`. Driver: the same repo checked out on Windows and inside WSL should aggregate costs/insights into one view while each location stays independently drillable — different branches, different dev servers, and different per-machine Claude configs (skills/agents/plugins/MCP).*

*Prerequisites shipped 2026-07-20 (PR #324) — slug-collision fix and P0 WSL attribution; details in `TODO.archive.md`.*

*P1 shipped 2026-07-21 — details in `TODO.archive.md`. **URL space is decided: a separate `/group/<slug>` namespace**, so every existing `/project/<slug>` keeps its current meaning and a group/project slug collision is intended. P3 must honour that.*
- [ ] **P2 — Aggregation layer** — pure `aggregateGroup(members)` implementing the four merge rules: repo-borne files **dedupe** (insight/issue IDs are stable; TODO items need content hashing) and surface divergence as signal; activity **sums**; location-bound state (branch, dirty, dev server, worktrees) **never merges**; environment-borne catalogs **diff**. Derived rates must recompute over the union — averaging two one-shot rates weights a 3-session location like a 100-session one. This is where double-counting bugs live; test heavily.
- [ ] **P3 — UI** — group card with a `2 locations` chip, Locations strip (per-checkout path/branch/dirty/dev-server), divergence flags on repo-borne tabs, per-location breakdown under every aggregate, and a new Environments tab comparing skills/agents/MCP across Claude homes (`source=user|plugin` = per-machine, `source=project` = repo-borne). A group of one must render exactly as today.

## Service Mode + Tray App

- [ ] **Signing + auto-updater + local release script** — Reopened 2026-07-19; **supersedes C5** of the service-and-tray plan. Full spec + status table: `docs/superpowers/plans/2026-07-19-signing-updater-release.md`. Key reframe: C5 deferred "signing + updater" as one money-gated task, but the updater's **minisign signing is free and independent** of OS code signing — an updater-only slice ships at zero cost today. Decisions: Azure Artifact Signing (~$120/yr, cloud HSM, no dongle, individuals US/CA — **not** EV, which stopped granting instant SmartScreen reputation in 2024) + existing Apple Developer Program ($99/yr, Tauri automates notarization). Traditional OV certs are now CI-incompatible (CA/B Forum hardware-key rule, June 2023). No single cert can sign both platforms — structurally impossible. Account signups are in MANUAL_STEPS.md and are the critical path (Azure validation takes 1–20 business days).
  - [x] **R** — `scripts/release-local.mjs` + `pnpm release:local`. Shipped 2026-07-19 (b8ba9e6), documented in `docs/help/tray-app.md`.
  - [x] **U** — Updater. Shipped 2026-07-19 (3d31ad5 + CI). U4 turned out to be a non-issue: the Windows updater exits via `std::process::exit()`, which never reaches the event loop, so `prevent_exit` can't swallow it — but that same hard exit runs no destructors, so the sidecar shutdown had to move into `on_before_exit` to stop an orphaned `node.exe` locking the installer out.
  - [x] **Publish the first updater-carrying release** — DONE 2026-07-21 with **v1.6.0** (tag on `24458fa`). The "Emit updater artifact" and "Publish updater manifest" CI steps ran for the first time and succeeded: `latest.json` published for all four platforms (`darwin-x86_64`, `darwin-aarch64`, `linux-x86_64`, `windows-x86_64`), each carrying a signature. **Confirmed end-to-end** — the update self-applied in place and the running server reports `1.6.0`. This is the first release to auto-update rather than needing a manual download.
  - [ ] **S** — Signing. **Dropped 2026-07-19** — decision: no cert spend for now. Apple Developer ID is already held and could be wired up as a follow-on (macOS half only, ~half the work: Tauri automates notarization once `APPLE_*` secrets are set). Windows would need Azure Artifact Signing (~$120/yr) and stays unstarted. Installers remain unsigned; SmartScreen/Gatekeeper warnings on first download are expected, and are unrelated to the (working) update verification.

## Cloud session ingest (Claude Code for web)

*Driver: sessions run on claude.ai/code produce **no local JSONL**, so they're invisible to every Minder read surface. Usage is currently light (small feature adds / bug fixes from away-from-desk) but expected to grow. The cost undercount is rounding error at this volume — the real damage is (a) a **gap in the project timeline** exactly where the away-from-desk work happened, and (b) a **dangling PR**: `ProjectDetail.tsx:170` builds `prSessionLinks` (`${repo}#${number}` → sessionId) from `/api/sessions` matched on `usageSlug`, so a PR opened from a web session has no session behind it. That cross-link degrades in proportion to web usage.*

*Prior art: [`simonw/claude-code-transcripts`](https://github.com/simonw/claude-code-transcripts) (Apache-2.0, Python). **Do not vendor** — ~90% of it is a JSONL→HTML renderer that Minder already beats (`adapters/claude.ts` + DB ingest + FTS5 + `/sessions`). The only interesting ~150 lines are the fetcher: `GET https://api.anthropic.com/v1/sessions` (list) and `GET https://api.anthropic.com/v1/session_ingress/session/{id}` (detail), with `Authorization: Bearer <oauth token>`, `anthropic-version: 2023-06-01`, `x-organization-uuid`; org UUID from `~/.claude.json` → `oauthAccount.organizationUuid`. **These endpoints are undocumented and are broken in the reference tool right now** — see [issue #77](https://github.com/simonw/claude-code-transcripts/issues/77).*

- [ ] **Spike — do the endpoints still work?** ~60 lines, scratchpad only, outside the repo. Read org UUID from `~/.claude.json`, take a token, call `/v1/sessions`, dump the response shape. Gates everything below: if #77 means the endpoints are dead for everyone, the design is moot for an hour's cost. If they work, the spike's real output is **the response schema** — `session_ingress` almost certainly does not return the local JSONL shape, and that mapping layer is the actual work in this feature.
- [ ] **Fetcher + disk sync** — `src/lib/cloudSessions/fetcher.ts`, modeled on `githubActivityCache`: `globalThis` singleton, TTL, `available:false` sentinel with a `reason` enum, never throws, never blocks a scan. **No background poller** — volume is low and human-paced, so sync on explicit user action (a "Sync cloud sessions" button) plus opportunistically on project-detail open. Hitting an undocumented endpoint on user intent rather than on a timer keeps the failure surface small. Materialize to `~/.minder/cloud-sessions/projects/<slug>/<sessionId>.jsonl` — same on-disk layout as a real Claude home, so a broken endpoint degrades to *stale but present* rather than *gone*.
- [ ] **`claude-cloud` adapter** — thin; reuses `parseSessionTurns` verbatim and tags `source: "claude-cloud"`. Deliberately **not** a network adapter: `SessionAdapter` is filesystem-shaped (`SessionFile.filePath`), and syncing to disk preserves that contract instead of widening the interface for one caller. The `source` field already threads through ingest/usage/sessions from Wave 10.2a, so classifier, cost calculator, one-shot detector, FTS5, `/costs` and the MCP tools light up with no changes.
- [ ] **Attribution — strict + explicit fallback** — a cloud session has no local path, so `usageSlug`/`dev-<name>` can't help; the only join key is `owner/repo` → the scanned `remoteUrl` (reuse `parseGitHubRemote`). Attribute only on an exact match; everything else lands in an "unattributed" bucket surfaced for manual assignment, persisted in `.minder.json`. **Rejected: fuzzy name/title matching** — `/costs` is the main consumer and a mis-attributed session silently corrupts a project's number, which is worse than a missing one. Fuzzy may return later as a *suggestion* in the assignment UI only. The repo-centric use case (small fixes → PRs) should make strict matching hit high coverage anyway.
- [ ] **Distribution risk — resolve before shipping to other users** — this is the blocker, not the engineering. Shipping a client for two undocumented endpoints inside a signed installer with an auto-updater means breakage lands in other people's installs and the support burden is ours. It also requires reading `~/.claude/.credentials.json`, which **violates the invariant stated in `src/lib/adapters/types.ts`** (*"Auth/credential files are never read"*) — fine as a personal choice, a changed promise when distributed. Multi-user also means token location, org UUID, and OS credential storage all vary per install (the reference tool's macOS-keychain path covers none of our Windows users). Minimum bar: **default-off feature flag** (new `FeatureFlagKey`, same discipline as `githubActivity`), explicit per-install opt-in with a consent string naming the file read and where transcripts are stored, retention/eviction policy for synced transcripts, and hard graceful degradation on endpoint change.

## Session-tooling review ports (9 PRs)

*From the 2026-07-28 review of five repos: [`madzarm/ccsearch`](https://github.com/madzarm/ccsearch), [`matt1398/claude-devtools`](https://github.com/matt1398/claude-devtools), [`codeaholicguy/ai-devkit`](https://github.com/codeaholicguy/ai-devkit), [`nikitadoudikov/claude-pulse`](https://github.com/nikitadoudikov/claude-pulse), [`cth9191/agentic-os-dashboard`](https://github.com/cth9191/agentic-os-dashboard). Filtered against the tree — most headline features are already shipped and in places Minder is ahead (Pulse guesses plan ceilings from observed limit hits; `quota.ts` reads the real `anthropic-ratelimit-unified-*` headers). User asked for all nine, each on its own PR.*

- [x] **#1 — RRF fusion for session search** — shipped 2026-07-28. `src/lib/data/rrf.ts` + refactored `searchSessionsInDb`. Replaces the hardcoded `titleScore = 0.5` merge, which made every title hit tie and imposed a meaningless 0.5 threshold between retrievers. Ordinal fusion, `k=60`, 3x candidate over-fetch, titles weighted 1.5x. Adapted from ccsearch's `src/search/rrf.rs`.
- [x] **#2 — Chunked full-turn FTS indexing** — shipped 2026-07-29. `src/lib/db/textChunks.ts` + writer-owned `prompts_fts` (schema v19, DERIVED_VERSION 12). Content **tier B** (prose + thinking, no tool I/O). Reverses the deferral at `schema.sql:502`. Also newly indexes **extended thinking** (was parsed for the `has_thinking` flag then discarded) and **subagent transcripts** (sidechain rows carry no preview column, so delegated work was invisible to search). **Measured** growth: `prompts_fts` 81.5 MB → ~233 MB, `index.db` 682 MB → ~834 MB (+22%) — 2.6x cheaper than the pre-build projection, because FTS overhead scales with row count, not bytes, and chunking makes rows fewer and fatter. *Known limit:* Codex/Gemini stay preview-indexed (`adapters/utils.ts` `TEXT_CAP` = 500 caps text inside the adapter, before ingest).
  - [ ] **Follow-up — widen `SessionAdapter` to carry full text** so Codex/Gemini sessions get full-body indexing too. Needs a contract change across all adapters + a DERIVED_VERSION bump; deliberately out of scope for #2.
- [x] **#3 — Per-turn context attribution** — shipped 2026-07-29 in PR #354 (`d089fb6`); the checkbox was missed at the time. claude-devtools attributes each turn's tokens across 7 categories (CLAUDE.md global/project/dir, skills, @-mentioned files, tool I/O, thinking, team overhead, user text) and plots it with compaction events marked. Distinct from `contextOverhead.ts` / `scanner/contextBudget.ts`, which are *static pre-flight* estimates (bytes→tokens over MCP servers + skills + memory); this is *measured, per-turn, over time*. Raw material already ingested (`db/ingest.ts` writes tool-uses; `sessionQuality.ts`/`sessionDiagnosis.ts` already reason about compaction). Landed as the per-session **Context** tab (`ContextAttributionPanel` + `GET /api/sessions/[sessionId]/context-attribution`).
- [x] **#4 — Blocking approval endpoint** — shipped 2026-07-29. `src/lib/approvals/{store,readOnlyTools,hookResponse,summarize}.ts` + `POST /api/hooks/permission` (blocking) + `GET /api/approvals` + `POST /api/approvals/[id]` + default-off `blockingApprovals` flag. Fail-open on every path (`ask`), replay-safe (409 on non-pending), bounded (swept deadlines), read-only **allowlist** bypass, session-scoped allow-all. Deliberately localhost/LAN only — not Pulse's ntfy relay. 38 tests.
  - [ ] **Follow-up — approval UI + LAN phone view.** The API surface and safety properties are done and tested; the dashboard **approval card** and `/phone`-style view are not. Until then the gate is drivable only via the endpoints, which is why the flag ships default-off. (Merged in #356: the `PreToolUse` hook registration, so the flag actually engages, and a Setup-page prompt when an older install lacks the blocking entry — `approvalHookRegistered`. Those were the discoverability half; the answer-it-from-a-phone half is what remains.)
- [x] **#5 — Notification rules engine** — shipped 2026-07-30. `src/lib/notifications/rules/{types,fields,matcher,cooldown,presets,validate,engine}.ts` + `NotificationRulesEditor.tsx` + default-on `notificationRules` flag. Evaluated in `/api/hooks` against **every** event, which is what makes `.env` access reachable at all (it arrives as `PreToolUse`, never as a notification). Six presets, all literal-operator so they're safe by construction. Curated field namespace rather than raw JSON paths (upstream renames would otherwise silently stop a security alert firing); per-leaf input caps so a `.env` path stays visible behind a 30 KB edit body; per-rule-per-project cooldown distinct from payload dedup. 82 tests. Also fixed a live bug: `PUT /api/config` replaces `notificationPrefs` wholesale, so the UI's single-key patch was wiping the sibling event's channels.
  - [ ] **Follow-up — token-usage rule fields.** Hook payloads carry no token counts, so `contains`/`gt` over cost or context fill isn't expressible in the hook-evaluated engine. Needs a polling evaluator over the usage index (`aggregator.ts`) feeding the same `matchRules` + cooldown path, so rules stay one concept with two trigger sources. Deliberately out of scope for #5 and called out as absent in `docs/help/notifications.md` rather than faked.
- [x] **#6 — Session → Markdown export** — shipped 2026-08-01. `src/lib/sessions/{markdownExport,exportReader}.ts` + `GET /api/sessions/[sessionId]/export` + a rebuilt `ExportModal`. **The premise in the original entry was half wrong**: a client-side exporter did exist, in `SessionDetailView.tsx`, and it was the reason to do the work rather than a reason to skip it — it rendered `data.timeline`, which caps assistant text at 300 chars (file path) / 500 (DB path), and emitted no code fences, so any session containing a fence produced a document whose structure collapsed partway through. The new one reads the session's own JSONL and takes only metadata from the index. Three detail levels; fences sized to exceed the longest backtick run in their own content; every truncation announced inline and tallied in an `Export notes` footer. When the JSONL is gone (retention, another machine, >50 MB, demo mode) the export degrades to the index and declares `fidelity: "index"` in front matter plus a warning callout, rather than 404ing or silently shipping previews. 53 tests.
- [x] **#7 — Cross-harness config drift detection** — shipped 2026-08-01. `src/lib/drift/{types,inventory,compare}.ts` + `scanner/driftLint.ts` + a new `drift` `LintTarget` + default-on `configDrift` flag. Detect-don't-write, as planned. Compares MCP servers / skills / instruction files across Claude, Codex, and Gemini. Two design calls carried the feature: **gaps summarize, conflicts stay per-item** (57 Claude skills vs 8 Codex skills is ~49 rows nobody reads as per-item findings), and **root instruction files compare as one artifact under three names** (`CLAUDE.md` ≡ `AGENTS.md` ≡ `GEMINI.md`; a filename match reports each as permanently missing forever). Smoke-tested against the real homes on this machine: 11 findings, no false positives. Caught a real bug doing so — `readdir({withFileTypes})` reports a symlink as `isSymbolicLink()`, never `isDirectory()`, and 24 of 57 `~/.claude/skills` entries are stow-links, so the first version silently dropped them and would have reported already-installed skills as missing *from Claude*. All findings P2 so a parity observation can never flip `hasBlocking`. 30 tests.
  - [ ] **Follow-up — project-scope drift.** Only user-scope config homes are compared. A repo's own `CLAUDE.md` vs `AGENTS.md` vs `.cursor/rules`, and `.mcp.json` vs a project-level Codex config, are the other half — cheap to add now that the compare layer is kind-agnostic, but it needs a per-project inventory source and a decision about where the findings hang (the per-project lint report rather than the global pass).
- [x] **#8 — Scheduled dispatch at quota reset** — shipped 2026-08-01. `src/lib/tasks/quotaGate.ts` (pure) + a gate in the dispatcher tick + default-on `quotaAwareDispatch` flag. The two systems were already there; the connection was missing. **Hold, don't reschedule** — nothing is written to `ops_tasks.scheduled_for`, so there is no stamped deadline to un-stamp if the reading was wrong and a user's own `scheduled_for` is never overwritten; rows stay `pending` and the next tick (~30 s) claims them. Waits for the **latest** reset among exhausted windows, not the earliest. Fails open on every unclear case (unconfigured, >30 min stale, future-dated, no reset time, reset in the past, reset >8 days out) — a gate that holds on bad data silently stops all background work, which is worse than one visible failed task. 19 tests.
  - [ ] **Follow-up — per-task `scheduleAtQuotaReset`.** The gate is global: it holds the whole queue. An explicit per-task affordance ("create this now, run it after the reset") would set `scheduled_for` from `resetAt` at creation time — a different thing, and the one case where writing the column is right because the user asked for that deadline.
  - [ ] **Follow-up — expose the 0.98 threshold in config.** Currently a constant with an options param used only by tests. Worth a `config.quotaGate.threshold` if the default turns out wrong in practice; deliberately not invented up front.
- [x] **#9 — Local embeddings + semantic search mode** — shipped 2026-08-01. `src/lib/embeddings/{quantize,model,store,search,backfill}.ts` + `GET/POST /api/embeddings` + a third RRF retriever + default-off `semanticSearch` flag. `@huggingface/transformers` added as an **optional** dependency (same posture as `better-sqlite3`). Every figure measured, not extrapolated: **58 MB** of int8 vectors at 384 B each (float32 would have been 241 MB — 29% on an 834 MB DB, for a default-off feature), **162 ms** for a full 157 146-vector brute-force scan, **~40 min** to embed the corpus at ~15 ms/chunk. Verified end-to-end against real transcript text with the real model — the top three hits for "the database migration is failing" contained the word "migration" zero times, which is the whole point. Vectors keyed on `(session_id, turn_index, chunk_index)`, never on the FTS rowid (reassigned on rebuild → would silently re-associate every vector with different text). Model cache redirected to `~/.minder/models/` because the library's default lives inside `node_modules` and a `pnpm install` would delete it. 42 tests.
  - [ ] **Follow-up — embed the query only when it looks semantic.** Every search currently pays one model inference (~20 ms) when the flag is on, including for a query that is plainly a filename or a slug where BM25 already wins.

## Housekeeping

- [ ] **Upgrade to Next.js ≥ 16.3 — unblocks the real fix for #284** — *Expected available early August 2026 (16.3 is in canary as of 2026-07-21; we're on `^16.2.10`).* The NFT whole-project-trace bailout (#284) has a known upstream answer: the `turbopackIgnore` comment goes on the **`fs` call**, not on the `path.join` — see [vercel/next.js#95125](https://github.com/vercel/next.js/issues/95125), where a Turbopack maintainer confirms the warning text's `path.join` is a *syntax* example, not the annotation target. Nested calls need it in both places: `readFileSync(/* turbopackIgnore: true */ path.join(/* turbopackIgnore: true */ dir, f))`. **This does not work on 16.2.10** — verified by annotating all 14 `fs` call sites in `claudeConversations.ts` (the module bisection identified as the poison) and measuring no change; the enabling fix is upstream PR #94361, which landed in `16.3.0-canary`, and `readdirSync` was only recently made annotatable. **On upgrade:** (1) re-apply the correct annotations to `src/lib/scanner/claudeConversations.ts` and confirm `api/adapters` drops from a full `src/` sweep via `.next/server/app/api/adapters/route.js.nft.json` entry count (**not** the build's warning count, which is noise); (2) if it works, the `outputFileTracingExcludes` list in `next.config.ts` added by #338 can potentially be narrowed back to just `dist/`+`src-tauri/target`; (3) also re-test the three pre-existing `turbopackIgnore` fixes in `db/migrations.ts`, `tasksDb/migrations.ts`, and `serverRoot.ts` — they annotate `path.join` and are likely inert. Full method + measurements on #284.
- [ ] **Capture a real worktree-overlay shot as `worktrees.png`** — *Mechanism shipped 2026-06-29; only the capture run + site swap remain.* `capture-screenshots.mjs` step 15 now self-discovers the first project with an active worktree overlay via `/api/projects` (option (a) — captures the feature in the wild) and shoots the Overview-tab **Worktrees** panel; when no worktree is active it skips without writing a file (so the site never references a missing PNG). **Remaining (needs a live worktree):** with an active `*--claude-worktrees-*` worktree present, run `pnpm capture:docs` to produce `site/screenshots/worktrees.png`, then point the `site/index.html` image at it (it currently honestly shows `todos-tab.png` at line ~105). The current capture run produces `todos-tab.png`, which honestly shows the TODOs tab. (Deferred alt — option (b): have the script `git worktree add` a throwaway demo worktree, capture, then tear it down — only worth doing if a reproducible, worktree-free capture is ever needed e.g. in CI.)
- [ ] **Bonus: PR check that warns when `site/screenshots/*.png` changes without `site/index.html`** — Sub-follow-up to the gh-pages publish work. A typical accident pattern: contributor regenerates screenshots but forgets to update the prose. Add a small workflow that runs on PRs touching `site/**` and fails (or warns via PR comment) if the diff contains `site/screenshots/*.png` changes but no `site/index.html` diff. Light-touch — a single grep over `gh pr diff --name-only`.

## agentic-os-dashboard ports (4 features)

*From the 2026-07-13 review of [`cth9191/agentic-os-dashboard`](https://github.com/cth9191/agentic-os-dashboard). Most of that dashboard's headline features Minder already does better (TokenBurn → `quota.ts` real rate-limit headers; skill dispatch + parallel queue → `tasks/spawner.ts` + `/swarms`). These four are the genuinely additive ideas. User asked for all four; #1 spiked 2026-07-14.*

- [x] **#1 — Live MCP server health strip** *(spike shipped 2026-07-14, verified end-to-end against real `~/.claude.json`)*. `src/lib/mcpHealth.ts` (pure probe: http/sse reachability, stdio PATH-launchability, PATHEXT-aware `resolveCommandOnPath`) + `src/lib/mcpHealthCache.ts` (poller mirroring `githubActivityCache`) + `GET /api/mcp-health` + `McpHealthIndicator` in the top bar + default-on `mcpHealth` flag. Tests in `tests/mcpHealth.test.ts`.
  **Follow-ups to promote the spike to full feature:**
  - [x] ~~Broaden the server source beyond user-scope `~/.claude.json`~~ — **done in PR #267** (Codex review): the route now reads the shared `getUserConfig()` merged reader (managed + `settings.json` + `~/.claude.json` + Desktop + plugin), so the strip reflects every configured server (verified: 20 servers surfaced incl. a genuinely-`down` one).
  - [x] ~~Optional real stdio handshake~~ — **done (follow-up D):** `src/lib/mcpStdioProbe.ts` spawns the server and does an `initialize` JSON-RPC round-trip, gated behind the **default-off `mcpHealthStdioProbe`** flag. env values are read **transiently** to launch the child and never stored/logged. `probeKind: "handshake"`. Tests in `tests/mcpStdioProbe.test.ts`.
  - [x] ~~Write `docs/help/mcp-health.md`~~ — **done (follow-up A):** `docs/help/mcp-health.md` (+ `public/help` mirror), slug added to `help-mapping.ts` + `HelpPanel`; opened from the popover's `?` via `useHelp().openHelp("mcp-health")`.
  - [x] ~~Click-through popover with `source`~~ — **done (follow-up B):** the strip is now a button opening a popover listing each server (status, transport + source chips, detail, `up · down · unknown` footer), sorted problems-first. `source` added to `McpHealth` and threaded through the probe/cache, so same-name servers disambiguate.
  - [x] ~~Immediate drop of removed servers~~ — **done (follow-up C):** `src/lib/mcpConfigWatcher.ts` watches `~/.claude.json` + `~/.claude/settings.json` and calls `invalidateUserConfigCache()` **only when the `mcpServers` slice changes** (a signature guard — `~/.claude.json` is rewritten constantly by Claude Code, so a naive watch would thrash the cache). Started idempotently from the route. Desktop/plugin/managed edits still surface within the 5-min TTL.
- [x] **#2 — Persistent burn HUD in the top bar** *(shipped 2026-07-14)*. `QuotaHud` in `AppTopbar` surfaces the more-utilized of the 5h/7d rate-limit windows (colour-coded %) + a projected `cap ~3:40 PM`; click-through popover shows all three windows (5h/7d/overage) with countdowns and schedule-aware projections. Projection math extracted to pure, unit-tested `src/lib/quotaProjection.ts` (reused by the refactored `QuotaBurndownChart`); reads the authoritative `anthropic-ratelimit-unified-*` headers via `loadQuota`/`useQuota` (not indexed usage turns — the header reflects real cross-client burn). Default-on `burnHud` flag; self-hides when quota isn't configured. Help doc `burn-hud`. Tests in `tests/quotaProjection.test.ts`.
- [x] **#3 — Demo mode (synthetic data)** *(shipped 2026-07-14)*. `MINDER_DEMO=1` env var OR default-off `demoMode` flag serve deterministic, now-anchored fixtures across every read surface. Fixtures in `src/lib/demo/` (projects/sessions/usage/catalogs/activity); guards sit above the DB/file branch at `scanAllProjects` (→ projects+board+insights+manual-steps+ops via ProjectData), the `data/index.ts` façade (sessions+usage), the agent/skill catalog loaders, and the 4 activity-strip routes (github/git/mcp/quota). 8 projects, ~17 sessions, ~$138 usage, 6 agents, 5 skills. Help doc `demo-mode`. Tests: `demoMode`/`demoProjects`/`demoFixtures`.
- [x] **#4 — Curated one-click "run this workflow" launcher chips** *(shipped 2026-07-15)*. A thin launcher on top of the task dispatcher: curated `LAUNCHER_WORKFLOWS` chips (review-diff/test-and-fix/typecheck-lint/update-changelog/tidy-todos/dependency-audit) + user-invocable skill chips, each dispatching one `POST /api/tasks` (scoped to a project via `metadata.projectPath`). Per-project "Quick Launch" strip on `/project/[slug]` + a global row (with project picker) under the top bar; default-on `workflowLauncher` flag; demo mode read-only (409). Enabler: `validateCreateTask` now threads `metadata` through (was dropped). `src/lib/launchers/definitions.ts`, `LauncherChips.tsx`, `GlobalLauncherRow.tsx`. Help doc `workflow-launcher`. Tests: `tests/launchers.test.ts` + `metadata` cases in `tests/tasksValidation.test.ts`. **Completes the four agentic-os-dashboard ports.**

## Memory Observatory

*Inspired by [Nicolas Bustamante's "Agent Memory Engineering"](https://x.com/nicbstme/status/2050301124314563025), comparing how Claude Code, Codex CLI, and Hermes implement agent memory. Key findings: clever architectures (vector DBs, knowledge graphs, dedicated memory agents) all lost to **LLM + typed markdown + bash**; the interesting axis is the read/write **discipline** the agent follows, not the data structure; memory is a **hint surface, not authority** — every body read should be wrapped in an age-aware "verify before recommending" reminder. Phase 1 (Freshness + Telemetry + Budget + MEMORY.md index awareness on `/memory`) is in active planning per the approved plan at `~/.claude/plans/i-recently-read-this-temporal-crane.md`; the entries below are the deferred follow-on phases, in priority order.*

- [ ] **Wave M.2 — Cross-harness memory bridge (Codex + Gemini)** — **Status (2026-05-12):** deferred after Phase 1 + M.1 + M.3 + M.4 shipped (PRs #118 / #119 / #121). M.2 is the only remaining Memory Observatory wave. **Plan doc**: see `C:\Users\joshu\.claude\plans\i-recently-read-this-temporal-crane.md` for the upstream framing (Nicolas Bustamante's "Agent Memory Engineering" article comparing Claude Code / Codex CLI / Hermes memory layers), the article principles → Project Minder opportunities table, and the Phase 1 → M.1 → M.4 → M.2 dependency rationale. The plan doc and this TODO entry are intentionally redundant on the M.2 spec itself; the plan doc adds the *why* (article framing, cross-wave dependency story) that's not duplicated here. Addresses the article's central frustration: memory rules taught to Claude Code don't transfer to Codex / Gemini despite sharing the same filesystem, because each model is post-trained on its harness's specific memory layer (Claude Code's typed taxonomy + always-loaded MEMORY.md index + age-aware `<system-reminder>` framing on every body read vs Codex's offline extract+consolidate pipeline + lazy `memory_summary.md` load + on-demand handbook reads). Extend the existing adapter registry from session-read to full memory IO, then ship a diff/sync UI so canonical rules can be authored once and pushed across stores. **Upstream research needed before PLAN**: (a) **Codex memory layout** — per the article, Codex's memory folder has its own `.git/`, a `memory_summary.md` (5K-token cap, injected per turn), a full `MEMORY.md` handbook, and a `skills/` tree; discover exact path under `$CODEX_HOME/`; document the `MemoriesUsageKind` enum values (`MemoryMd`, `MemorySummary`, `RawMemories`, `RolloutSummaries`, `Skills`) so the bridge can expose per-kind usage. (b) **Gemini memory layout** — less documented; audit `~/.gemini/` for any memory equivalent on Windows; may need read-only first if Gemini has no equivalent yet. (c) **Taxonomy mapping** — Claude's `feedback_*` / `project_*` / `reference_*` / `user_*` may not align 1:1 with Codex's structure; define a canonical internal type set and a per-adapter encoder/decoder. **Interface extension**: extend `SessionAdapter` in `src/lib/adapters/types.ts` with `discoverMemory(opts?: { projectPath?: string }): Promise<MemoryEntry[]>`, `writeMemory(entry: MemoryEntry): Promise<MemoryWriteResult>`, and a `capabilities: { memoryRead: boolean; memoryWrite: boolean }` flag so the UI can grey-out write actions on adapters that don't support them. **UI**: new page `/memory/bridge` with a three-column layout (Claude | Codex | Gemini), each row showing the entry's presence across stores. Filter "Only show divergent" (exists in one store but not others, or bodies differ). Per-row actions: `Push to Codex`, `Push to Gemini`, `Pull from Codex`, etc., each using the target adapter's `writeMemory()` with format translation. Conflict UI: side-by-side diff with manual resolution before write. **Critical files**: `src/lib/adapters/types.ts` (extend interface; define adapter-agnostic `MemoryEntry` shape), `src/lib/adapters/claude.ts` (implement `discoverMemory()` / `writeMemory()` as thin wrappers over `src/lib/scanner/memoryWriter.ts`), `src/lib/adapters/codex.ts` (NEW work based on upstream research), `src/lib/adapters/gemini.ts` (NEW work or mark `memoryWrite: false` if upstream lacks it), `src/lib/memory/bridgeDiff.ts` (NEW — cross-adapter join + diff), `src/app/memory/bridge/page.tsx` (NEW), `src/app/api/memory/bridge/route.ts` (NEW — `GET` returns diff, `POST` performs a push), `src/components/MemoryBridge.tsx` (NEW — three-column UI). **Open questions to resolve in DISCUSS**: split into read-only first (M.2a) and write-mode second (M.2b)? how to handle harness-specific body syntax (e.g., Claude's `<system-reminder>` framing) when pushing to Codex — strip, transform, or warn? per-store last-modified for "newer side wins" auto-resolution, or always require manual conflict? per-project bridge or user-scope only initially? **Dependencies**: Phase 1 (read telemetry + index parsing) and ideally M.1 (seed generator) ship first — the bridge needs a canonical view of "what should be in every store." **Roadmap order**: ships after M.1. Ref: article §"Why memory does not transfer cleanly between agents" + §"Codex: only the index, full handbook on demand" + §"Claude Code: full index always loaded, bodies on demand."
