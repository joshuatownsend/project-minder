// Vitest stub for the `server-only` Next.js package. Production builds have
// the real module that throws at compile time if a client component imports
// a server-only file; tests run in plain Node where the import would fail
// to resolve. This file is empty — its purpose is to satisfy the import.
export {};
